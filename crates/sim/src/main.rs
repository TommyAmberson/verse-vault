//! Phase 4 stub. The legacy simulation binary depended on the graph layer and
//! card-types TOML, both of which have been deleted. Phase 7 rewrites the sim
//! against the new per-test HSRS engine.

mod cache;
mod learner;
mod metrics;

fn main() {
    println!("verse-vault-sim: phase 4 stub (rewrite pending in phase 7)");
}
