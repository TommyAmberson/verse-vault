//! Phase 4 stub: the real-data integration test exercised the legacy graph
//! layer + card_types TOML, both deleted. Phase 7 reintroduces this against
//! the per-test HSRS engine.

#[test]
#[ignore]
fn build_graph_from_corinthians() {
    // Phase 4: legacy graph layer deleted; rewrite pending in Phase 7.
}

#[test]
#[ignore]
fn engine_from_corinthians() {
    // Phase 4: legacy engine deleted; rewrite pending in Phase 7.
}
